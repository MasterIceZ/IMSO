#include <iostream>
#include <string>

const int MxN = 3030;

int a[MxN];

signed main(int argc, char *argv[]) {
	std::cin.tie(nullptr)->std::ios_base::sync_with_stdio(false);
	int n, m, k;
	std::string s;
	std::cin >> n >> m >> k >> s;
	s = " " + s;
	return 0;
}
