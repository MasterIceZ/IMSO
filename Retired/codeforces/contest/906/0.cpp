#include <bits/stdc++.h>

using namespace std;
using ll = long long;

inline void runcase() {

  return;
}

int main() {
  cin.tie(nullptr)->ios::sync_with_stdio(false);
  int t = 1;
  cin >> t;
  while(t--) {
    runcase();
    cout << "\n";
  }
  return 0;
}