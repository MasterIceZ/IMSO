#include <bits/stdc++.h>

using namespace std;

int main() {
	cin.tie(nullptr)->ios::sync_with_stdio(false);

	int n;
	cin >> n;
	vector<vector<int>> v(2 * (n + 1) + 1, vector<int> (2 * (n + 1) + 1, 0));

	return 0;
}
